# Frontend env for Electron build
# When loading from file:// protocol, Vite proxy is not available
# Frontend must call backend via absolute URL
VITE_API_BASE=http://localhost:3001

# Sensitivity config
VITE_INTERIM_DEBOUNCE_MS=500
VITE_FINAL_SILENCE_MS=1000
